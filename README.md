# Pneumonia-Detection-Test-using-CNN-Model-
The Pneumonia Detection Test using CNN Model is an AI-based system that analyzes chest X-ray images to detect the presence of pneumonia. Using Convolutional Neural Networks (CNNs), the model automatically learns and extracts critical features from X-ray scans.
